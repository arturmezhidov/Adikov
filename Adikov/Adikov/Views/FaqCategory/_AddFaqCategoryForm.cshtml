@model Adikov.ViewModels.FaqCategories.CategoryViewModel

@using (Html.BeginForm("Add", "FaqCategory", null, FormMethod.Post, new { id = "faq-category-form", @class = "form-horizontal", role = "form" }))
{
    <div class="form-body">
        <div class="form-group">
            <label class="col-md-3 control-label">Название <span class="required" aria-required="true"> * </span></label>
            <div class="col-md-6">
                @Html.TextBoxFor(m => m.Name, new { @class = "form-control", placeholder = "Название" })
            </div>
        </div>
        <div class="form-group">
            <label class="col-md-3 control-label"> </label>
            <div class="col-md-6">
                <label class="mt-checkbox">
                    @Html.CheckBoxFor(m => m.IsPublished) Опубликовать
                    <span></span>
                </label>
            </div>
        </div>
    </div>
    <div class="form-actions">
        <div class="row">
            <div class="col-md-offset-3 col-md-9">
                <button type="submit" class="btn green">Добавить</button>
                <button type="reset" class="btn default">Очистить</button>
            </div>
        </div>
    </div>
}