@model Adikov.ViewModels.FaqCategories.CategoryViewModel

<div class="portlet light">
    <div class="portlet-title">
        <div class="caption">
            <i class="fa fa-pencil font-red-sunglo"></i>
            <span class="caption-subject font-red-sunglo bold uppercase"> Редактирование категории - @Model.Name </span>
        </div>
        <div class="actions">
            <a href="@Url.Action("Requests", "Faq")" class="btn btn-circle red-sunglo ">
                <i class="icon-question"></i> Запросы
            </a>
            <a href="@Url.Action("Index", "Faq")" class="btn btn-circle red-sunglo ">
                <i class="icon-home"></i> Домой
            </a>
        </div>
    </div>
    <div class="portlet-body">
        @Html.Partial("_EditFaqCategoryForm", Model)
    </div>
</div>
