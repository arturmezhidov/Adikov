@model Adikov.ViewModels.ProductAttribute.ProductAttributeViewModel

@using (Html.BeginForm("Edit", "ProductAttribute", null, FormMethod.Post, new { id = "product-attribute-form", @class = "form-horizontal", role = "form" }))
{
    @Html.HiddenFor(m => m.Id)
    <div class="form-body">
        <div class="form-group">
            <label class="col-md-3 control-label">Название <span class="required" aria-required="true"> * </span></label>
            <div class="col-md-4">
                @Html.TextBoxFor(m => m.Name, new { @class = "form-control", placeholder = "Название" })
            </div>
        </div>
    </div>
    <div class="form-actions">
        <div class="row">
            <div class="col-md-offset-3 col-md-9">
                <button type="submit" class="btn green">Сохранить</button>
                <a class="btn default" href="/ProductAttribute/Index">Отмена</a>
            </div>
        </div>
    </div>
}